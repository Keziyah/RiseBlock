# Rise Block: Knowlesge Check

## How to use

Clone, `npm run build` then `npm run start`

The React component is in app/components/Quiz.jsx, and most styles are in app/components/sass/stylesheets/components/_quiz.scss. 
